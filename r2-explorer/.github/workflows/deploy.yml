name: Deploy R2-Explorer

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Phase notice
        run: echo "Worker is implemented in Phase 5. CI deployment automation remains scheduled for Phase 7."
